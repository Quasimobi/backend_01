---
---
{
  "slides": [
    {% for post in site.tags.carousel_02 %}
      { 
        "foto_bestand": "{{ post.foto_bestand }}",
        "foto_alt":     "{{ post.foto_alt }}",
        "tekst_01":     "{{ post.tekst_01 }}",
        "tekst_02":     "{{ post.tekst_02 }}",
        "sterren":      "{{ post.sterren }}"
      } {% unless forloop.last %},{% endunless%}
    {% endfor %}
  ]
}
